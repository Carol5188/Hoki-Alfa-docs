# ACM 运营后台管理

 

最后核验日期：2026-08-19

## 适用人群

本篇面向负责 ACM 开通、账号治理、模板配置、团队迁移、公会归属和运营审计的后台运营与产品人员。文中的高风险操作都应先核对对象和影响范围，再使用页面预览和正式提交。

## 功能说明

ACM V3 运营后台是现行团队治理入口，覆盖根团队、成员账号、公会归属、模板、权限、额度、运营负责人和操作记录。后台工作台以运营负责人树和全局搜索为主要入口；用户可见范围由后台授权和运营负责关系共同决定。
后台治理改变的是当前组织和后续业务能力。已经冻结的历史周期归属、统计和工资不会因当前结构调整而重算。

## 后台工作台

| 能力     | 业务用途                  | 使用边界               |
| ------ | --------------------- | ------------------ |
| 运营负责人树 | 按负责人查看其关联的根团队和成员结构    | 负责人关系不等于工资归属       |
| 全局搜索   | 按身份信息查找根 ACM、成员或公会    | 可搜索范围仍受后台授权限制      |
| 团队详情   | 查看结构、账号、公会、权限和额度      | 只反映当前关系，历史需到相应记录查询 |
| 操作记录   | 追溯创建、迁移、停用、权限和额度等治理动作 | 用于审计，不能替代正式周期报表    |

## 根团队建立

### 创建根 ACM

1. 核对目标业务线、大区、根账号身份和运营负责人。
2. 选择与团队业务相符的模板。
3. 创建根 ACM 并绑定正确 UID。
4. 确认账号启用状态、默认权限和团队额度规则。
5. 进入团队详情核对根层级、负责人和用户端入口。
   
   ### 现有后台账号加入 ACM
   
   当前后台账号可按业务需要加入 ACM，成为根 ACM 或团队成员。操作前应确认：
+ 后台登录身份与需要绑定的用户端 UID 是同一业务责任人。
+ 账号未处于冲突的 ACM 关系中。
+ 目标团队、层级和上级关系符合模板。
+ 加入后不会导致自己管理自己的循环关系。
+ 后台权限与 ACM 业务权限分别评估，不因加入而默认互相继承。
  
  ## 成员账号管理
  
  ### 创建下级成员
  
  后台可在指定上级下创建成员。目标层级必须启用，且模板允许该上级建立这种下级关系。创建成功后还需核对 UID 绑定、账号状态和最终权限，不能只以成员出现在树中作为开通完成。
  
  ### UID 治理
  
  | 操作                             | 操作前确认               | 成功判断                   | 不可自行处理的异常           |
  | ------------------------------ | ------------------- | ---------------------- | ------------------- |
  | 绑定                             | UID 属于目标责任人且未被占用    | 用户端可识别正确 ACM 身份        | UID 已被未知账号占用或身份无法核实 |
  | 换绑                             | 原绑定原因、新 UID 身份和影响入口 | 新 UID 生效，旧 UID 不再进入该账号 | 换绑后出现跨团队身份冲突        |
  | 解绑                             | 账号是否仍需用户端操作、是否同时停用  | 用户端不再识别该账号             | 解绑对象与历史责任人不一致       |
  | UID 操作不会迁移历史工资，也不应用于修正过去周期的归属。 |                     |                        |                     |
  
  ### 启用、停用与删除
  
  停用前必须选择依赖对象的处理方式：
  
  | 模式                                                                              | 操作前确认              | 影响范围               | 成功判断            |
  | ------------------------------------------------------------------------------- | ------------------ | ------------------ | --------------- |
  | 仅停用当前账号                                                                         | 下级成员和公会是否可继续保持有效关系 | 当前账号               | 账号不可使用，依赖关系符合预期 |
  | 转移依赖后停用                                                                         | 接收人有效、区域一致、模板关系合法  | 当前账号及其直属成员、公会的当前归属 | 依赖转移成功后账号停用     |
  | 停用下级团队                                                                          | 已确认整支团队停止业务        | 当前账号及全部下级          | 树中目标范围均进入停用状态   |
  | 停用时可选择同时解绑 UID。删除非根成员时，可以把直属成员和直属公会转给其上级，再删除当前成员。若接收关系不合法、区域不一致或仍有未处理依赖，不能强行完成。 |                    |                    |                 |
  | 删除后历史记录仍保留。不要以删除账号作为“清理错误工资”或“重建历史数据”的方法。                                       |                    |                    |                 |
  
  ## 组织结构调整
  
  ### 统一操作原则
  
  所有结构调整均采用“预览影响，再正式提交”的两阶段方式：
1. 选择来源对象、目标对象和调整方式。
2. 查看涉及的成员、公会、团队和权限影响。
3. 核对接收人、区域、模板和依赖对象。
4. 确认业务理由后提交。
5. 系统按提交时的最新状态重新校验。
6. 成功后检查团队树、公会归属和操作记录。
   
   ### 同团队更换上级
   
   用于在同一根团队内调整成员汇报关系。需保证新上级不是该成员自己或其下级，且新父子关系被模板允许。若成员携带下级团队和公会，预览中应完整展示影响范围。
   
   ### 跨团队转移
   
   用于把成员及指定依赖迁入另一根团队。操作前至少确认：
+ 来源与目标团队的大区和业务规则兼容。
+ 目标模板能容纳成员当前或调整后的层级。
+ 下级成员、直属公会和权限如何处理。
+ 目标团队额度和账号状态满足要求。
+ 当期组织变化的生效时间已与区域运营确认。
  批量转移应先用小范围结果核对标准，再执行整批。某一对象失败时按页面结果逐项处理，不默认整批全部成功。
  
  ### 提升为根 ACM
  
  将现有成员从原团队拆出并建立为新根团队。需明确其携带哪些下级和公会、新团队使用哪个模板、由哪些运营负责人负责，以及原团队剩余结构是否仍合法。
  
  ### 根 ACM 降为成员
  
  将一支根团队并入另一团队。影响通常覆盖原根账号、整支下级和公会，属于高风险操作。目标上级、目标模板和层级必须能承接完整结构；若不能，应先制定分步迁移方案。
  
  ### 修改成员层级
  
  层级变更会影响称呼、允许的父子关系、默认权限和后续可创建对象。操作前应先预览现有上下级关系是否仍合法。若模板无法承接现状，先处理依赖关系，不能只改显示层级。
  
  ## 公会归属管理
  
  | 操作                                                                       | 适用场景             | 操作前确认              | 历史影响           |
  | ------------------------------------------------------------------------ | ---------------- | ------------------ | -------------- |
  | 创建公会                                                                     | 后台代为完成合资格建会      | 用户身份、区域、接收成员和建会资格  | 从实际成功时间起进入当前关系 |
  | 绑定公会                                                                     | 将现有公会纳入正确 ACM 归属 | 公会未被冲突关系占用、成员有效    | 不自动补写已冻结历史     |
  | 转移公会                                                                     | 更换单个公会直接归属人      | 来源、接收人、区域和生效时间     | 已冻结周期不变        |
  | 批量转移                                                                     | 大规模责任人调整         | 清单准确、接收关系一致、失败处理方案 | 已冻结周期不变        |
  | 公会转移成功后，应同时核对来源成员当前公会列表、目标成员当前公会列表和操作记录。若正在处理历史报表差异，不应通过反复转移当前归属来修正过去数据。 |                  |                    |                |
  
  ## 团队模板管理
  
  ### 模板状态与删除边界
  
  | 模板类型或状态  | 可执行动作      | 限制            |
  | -------- | ---------- | ------------- |
  | 受保护默认模板  | 查看、按允许范围维护 | 不可删除          |
  | 自定义未使用模板 | 编辑、复制或删除   | 删除前再次确认没有团队引用 |
  | 自定义已使用模板 | 编辑需预览影响    | 使用中不可直接删除     |
  
  ### 修改模板
  
  模板变更需先进行影响预览。以下变化尤其需要谨慎：
+ 删除或停用现有团队正在使用的层级。
+ 取消当前已存在的父子关系。
+ 收回大量账号正在使用的默认权限。
+ 改变团队默认额度规则。
+ 调整会影响建会确认方式的业务规则。
  现有关系可能阻止破坏性修改。涉及层级迁移时，应根据最新预览明确确认，不能使用过期预览结果直接执行。
  
  ## 权限管理
  
  ### 模板默认与账号差异
  
  | 权限来源                                            | 作用            | 使用建议            |
  | ----------------------------------------------- | ------------- | --------------- |
  | 模板默认权限                                          | 为同层级账号提供统一基线  | 稳定、普遍适用的能力放在模板中 |
  | 账号允许差异                                          | 为个别账号增加可下放能力  | 仅用于有明确业务理由的例外   |
  | 账号禁止差异                                          | 从个别账号收回某项默认能力 | 用于风险控制或职责差异     |
  | 子账号不能获得模板或上级不可下放的能力。调整后应以账号最终权限预览为准，并核对用户端实际入口。 |               |                 |
  
  ### 功能权限与数据范围
  
  后台配置时必须分别回答两个问题：
1. 该账号能否执行添加成员、创建公会、封禁、奖励、审核等动作？
2. 该账号的数据中心和后台报表能看到自己、下级团队、负责团队还是全局？
   只回答其中一个会造成“有按钮但看不到对象”或“看得到数据却误以为能修改”的运营误判。
   
   ## 额度管理
   
   额度以根团队为共享单位，当前重点动作包括添加成员、创建公会和封禁用户。
   
   ### 有效规则判断
3. 先查看根团队是否有个别额度规则。
4. 有个别规则时，优先使用根团队规则。
5. 没有个别规则时，使用模板默认规则。
6. 两者都没有有效规则时，该动作按不限额处理。
   额度可按日、按月或累计计算。不限额只代表不受数量上限约束，不代表跳过权限、资格和状态校验。
   
   ### 重置当前用量
   
   重置用量是高风险动作，必须：
+ 先预览将重置的动作、周期和当前使用量。
+ 确认不是统计延迟或多人并发造成的误判。
+ 填写明确业务理由和操作人信息。
+ 提交后核对新用量，并保留审计记录。
  后台治理动作和用户端业务动作不一定使用同一额度消耗规则。不能仅凭后台成功创建就推断用户端额度应当减少，核对时以页面显示的有效规则和操作记录为准。
  
  ## 运营负责人管理
  
  根团队可关联多名运营负责人。调整负责人前应确认团队交接、报表可见范围和日常责任边界。负责人变更不修改团队层级、公会直接归属或历史工资。
  
  ## 高风险操作检查表
  
  | 操作     | 操作前确认             | 成功判断               | 需要升级处理的情况         |
  | ------ | ----------------- | ------------------ | ----------------- |
  | 跨团队迁移  | 来源、目标、层级、下级、公会和区域 | 新团队结构完整，旧团队无残留错归属  | 部分成功且页面无法安全重试     |
  | 根升降级   | 整支团队影响和目标模板承载能力   | 根关系、负责人、下级和公会均符合预期 | 出现循环关系或两支根团队混合归属  |
  | 批量转移公会 | 清单、接收人、区域和生效时间    | 每个公会仅有一个当前归属       | 同一公会出现在多个结果中      |
  | 停用下级团队 | 完整成员和公会范围         | 目标范围状态一致           | 非目标团队被影响          |
  | UID 换绑 | 新旧身份和占用情况         | 用户端身份与后台责任人一致      | 换绑后出现错人或跨团队身份     |
  | 重置额度   | 周期、动作、用量和理由       | 用量按预览结果更新并有记录      | 重置后出现重复业务结果       |
  | 模板迁移   | 最新影响预览和关系合法性      | 现有团队结构和权限均可解释      | 大量账号入口异常且无法回滚业务配置 |
  
  ## 业务规则
+ 后台治理以当前组织为对象，不通过组织操作修改历史周期结果。
+ 结构性操作必须先预览，提交时再按最新状态校验。
+ 接收人必须有效，区域、模板和父子关系必须合法。
+ 模板、账号权限、数据范围、运营负责人和报表范围应分别管理。
+ 删除、停用和解绑是不同动作，应明确是否需要组合执行。
+ 所有高风险动作都应以页面成功结果、变更后的对象状态和操作记录三方核对。
  
  ## 常见问题
  
  ### 为什么后台预览显示可迁移，正式提交却失败
  
  提交会重新检查最新状态。预览后如果成员、公会、模板、接收人或额度发生变化，系统会拒绝按旧条件执行。
  
  ### 停用账号时应该选哪种模式
  
  取决于下级和公会是否继续业务。只暂停本人选仅停用；需要保留团队连续性应先转移依赖；整支团队结束业务才使用停用下级团队。
  
  ### 为什么模板层级不能删除
  
  通常是仍有团队或成员使用该层级，或删除后会使现有父子关系失效。应先查看影响预览并处理使用中的关系。
  
  ### 为什么账号已经删除，历史报表里还存在
  
  历史报表保留周期当时的冻结身份和归属，用于财务和业务追溯。当前删除不能抹除已经发生的历史事实。
  
  ### 运营负责人能否直接看到所有 ACM 报表
  
  只能看到其负责或被授权的范围。跨团队全局报表还需要独立的报表全局权限。
  
  ## 关联章节
+ [ACM 业务全景与角色权限](01-ACM业务全景与角色权限.md)
+ [ACM 工作台与日常操作](02-ACM工作台与日常操作.md)
+ [ACM 统计快照与工资逻辑](05-ACM统计快照与工资逻辑.md)
+ [ACM 报表导出与异常处理](06-ACM报表导出与异常处理.md)
+ [ACM 功能手册导航](00-ACM功能手册导航.md)
